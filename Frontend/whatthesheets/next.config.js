/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.steamboat.com', 'www.steamboatspringsboatrentals.com', 'www.colorado.com'],
      }
}

module.exports = nextConfig
